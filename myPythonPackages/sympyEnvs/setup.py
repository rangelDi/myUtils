from setuptools import setup, find_packages

setup(
    name='sympyenvs',
    version='1.0',
    description='A tiny sympy environment wrapper',
    author='milo',
    packages=find_packages(),
    zip_safe=False
)
