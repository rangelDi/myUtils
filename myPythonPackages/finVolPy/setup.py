from setuptools import setup, find_packages

setup(
    name='finvolpy',
    version='0.1',
    description='a tiny (and slow) finite volume package',
    author='rangelDi',
    packages=find_packages(),
    zip_safe=False
)
